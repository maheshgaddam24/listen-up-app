import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.listenup.starter',
  appName: 'Listen-Up',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
